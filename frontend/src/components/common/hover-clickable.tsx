import { Stack } from "@mui/material";
import { ReactNode } from "@types/react";

export const HoverClickable = ({ children }: { children: ReactNode }) => {


        return <Stack>
            
        </Stack>
};
